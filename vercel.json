

{"comment_1": "should be fixed, so only the main production site is index, not the preview URL's",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "x-robots-tag",
          "value": "index, follow"
        }
      ]
    }
  ]
}